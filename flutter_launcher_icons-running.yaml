flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon_running/app-running.png"
  # Icons with alpha channel are not allowed in the Apple App Store.
  #Set "remove_alpha_ios: true" to remove it
  remove_alpha_ios: true